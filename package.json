{
  "name": "taiwan-wanderings", "version": "1.0.0", "private": true,
  "description": "台灣漫遊錄 — HTML5 anime exploration adventure with AI companion dialogue",
  "type": "module", "engines": { "node": ">=22" },
  "scripts": { "start": "node --env-file-if-exists=.env server.mjs", "test": "node --test tests/*.test.mjs", "check": "node --check server.mjs && node --check dist/game.js && node --check dist/engine.js && node --check dist/levels.js" }
}
