# 台灣漫遊錄 — 遊戲設計與實作說明

## 定位與完整遊玩循環

繁體中文、單人、約 15–25 分鐘的動漫場景點擊冒險。玩家收到一本空白漫遊錄，與原創角色小嵐找回五枚印章。開啟遊戲即進入九份探索；不設廣告首頁、登入或付費門檻。

每章：閱讀旅伴開場 → 點擊三個探索點 → 線索收錄手帳 → 進入謎題 → 答錯重試或取得提示 → 成功收集唯一印章 → 下一章。全部通關後呈現結局，可重讀或回看任一章。

## 美術與操作

深海藍遊戲外框、金橙燈火與原創動漫插畫。左側旅程軸，中央場景探索，底部旅伴對話。手機將章節列表改為可橫向捲動，探索點移到標題與任務卡中間。原生 dialog 提供焦點限制、Esc 關閉；按鈕可用鍵盤操作。尊重 reduced-motion。Canvas 僅繪製環境光點，Web Audio 產生短音效，不需外部音樂。

場景圖：九份獨立背景；四地圖集包含台北、日月潭、台南、花蓮等面板；對話使用小嵐半身像。圖片均本地供應，無遠端素材依賴。

## 狀態與存檔

`engine.js`：純資料邏輯。`game.js`：DOM、互動與 localStorage。`levels.js`：內容。`server.mjs`：AI 與靜態供應。

存檔鍵 `taiwan-wanderings-v1`，version 1。含當前章節、已完成章節、各章探索線索、最近 20 則各章對話、共同回憶與音效設定。不儲存 API 金鑰。

只承認從第 0 章開始連續完成的章節。讀檔時限制索引、過濾非法對話角色、限制文字長度、去除重複線索及共同回憶。無法存檔時告知玩家。不支援雲端同步。

客戶端存檔不是防作弊機制。遊戲為單人單機作品，沒有排行榜或付費道具；模型回應不能直接觸發狀態寫入。原生按鈕及引擎規則控制正常遊玩流程。

## AI 契約

`GET /api/config` → `{ aiEnabled: boolean }`，不回傳憑證。

`POST /api/chat` → `{ level, clues, completed, message, history }`。

驗證：章節存在、訊息 1–500 字元、本文最多 18 KB；最多 8 則歷史、每則最多 1500 字元；移除所有客戶端 system/developer 訊息。伺服器從章節資料建立小嵐 persona，加入玩家已找到的線索。完整線索之前只引導探索；完整之後可提供答案提示。

供應商為可設定的 Chat Completions 相容服務。回覆格式 `{ reply, mode: 'ai' }`。所有回應以 textContent 呈現，不注入 HTML。伺服器不回傳供應商錯誤本文或金鑰。提示詞是角色指引，不能保證模型永不偏離角色；因此模型沒有操作工具或遊戲狀態修改權限。

若未設定 AI，或請求失敗，使用已標示的離線劇情。切換關卡／重開遊戲時取消進行中請求，防止舊回覆寫到新章節。離線模式是 keyword-based scripted dialogue，不能冒稱 LLM。

## 未包含的能力

沒有自由走動、戰鬥、多人、帳號、雲端存檔、語音辨識、AI 圖片即時生成、付費交易或行動商店封裝。這些都不是目前遊戲所需。

## 擴充

添加新章節時更新 `LEVELS`、章節名稱、章節總數限制與存檔版本。新謎題可擴充 `puzzle.kind`、呈現器、答案驗證及對應測試。若要大型多人公開版本，先把身分與用量管理接入伺服器，不應將服務端金鑰移入前端。
